# ─────────────────────────────────────────────────────────────────────────────
# MacBook environment — E01–E08 (Phase 1 & 2)
# Runs on Apple Silicon (MPS) or Intel CPU
# Does NOT include vLLM (Linux + CUDA only)
#
# Setup:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-mac.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core ML
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
accelerate>=0.29.0          # needed for multi-device / device_map

# HuggingFace utilities
huggingface_hub>=0.22.0     # model downloads, caching
tokenizers>=0.19.0          # fast tokenizers (ships with transformers but pin)
datasets>=2.18.0            # ShareGPT and other benchmark datasets

# Numerics / data
numpy>=1.26.0
scipy>=1.12.0               # used in latency percentile calculations

# Async / HTTP (used in client scripts)
aiohttp>=3.9.0
requests>=2.31.0

# Utilities
tqdm>=4.66.0                # progress bars
rich>=13.0.0                # pretty terminal output (optional but nice)
